# creating list
ls = ['hello', 'wahab', 33, True, None]   #can contain any type of data 
print(ls)

# we element of list 
print(ls[0])
print(ls[3])

# changing  value of list
ls[1] = 'zaynab'
ls[3] = False

print(ls)


# creating a tuple 
tp = (1, 'wahab', True, 44)
print(tp)

# access the value of tuple 
print(tp[0])

# try to chagne the value of tuple 
# tuple[1] = 'this'  #it will give an error beacuse tuple is immutable means we cannot change the vlaue of tuple once created.

